package main

import "github.com/yourbasic/graph"

func reshape(pixels [][]Pixel, g graph.Mutable, scale int, genSVG bool) {
	// width := len(pixels)
	// height := len(pixels[0])
	// scale := 50

	if genSVG {
		//	generateSVG(pixels, g, SvgConfig{"./results/2.solveAmbiguities", true, true})
	}

}
